name: 🚀 Update from Modular ZIP

on:
  push:
    paths:
      - 'trigger_update.txt'

jobs:
  update-project:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Download latest ZIP
        run: |
          curl -L -o latest_ir_tool.zip https://chatexport.s3.amazonaws.com/ir_tool_modular_final_updated.zip
          unzip -o latest_ir_tool.zip -d updated_content
          rm -rf core data pages Home.py requirements.txt
          cp -r updated_content/* ./
          rm -rf updated_content latest_ir_tool.zip

      - name: Commit & Push
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "🔄 Auto-update from ZIP package"
          git push
